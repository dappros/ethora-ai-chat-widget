// @emoji-mart/react stub (the reaction emoji Picker). Never mounted when
// config.disableInteractions is set. Returns null so the named/default import
// resolves without pulling the picker + its dataset into the bundle.
export default function Picker() {
  return null;
}
