// @emoji-mart/data stub. The full emoji dataset (~400KB) is only used by the
// reaction emoji Picker, which the assistant disables (config.disableInteractions
// makes MessageInteractions render null, so the Picker never mounts). Stubbing
// drops the dataset from the bundle.
export default {} as any;
export const Emoji = undefined as any;
